# HARNESS.md

이 파일은 `AGENTS.md` §43(모델 하네스)·§44(목표 완결과 완성 기준)의 전체 원문입니다. 코드·기능을 변경하는 작업(사소한 한 줄 수정, 순수 질의응답, 즉시 자명한 기계적 편집은 예외)에서는 §4 작업 시작 전 필수 절차에 따라 이 파일을 읽고 적용합니다.

이 하네스는 사용자의 에이전틱 코딩 툴 전역 설정 5곳(`~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`, `~/.config/opencode/AGENTS.md`, `~/.config/kilo/AGENTS.md`, `~/.zcode/AGENTS.md`)에도 같은 내용으로 동기화되어 있습니다. 이 저장소가 하네스 원본이며, 한 곳을 고치면 나머지도 함께 갱신해야 합니다. 전역 설정이 없는 협업자나 다른 환경에서는 이 파일이 하네스를 적용받는 유일한 경로입니다. 전역 5곳에는 §43·§44가 하나의 '작업 기준' 절로 합쳐져 있고, §44.1·§44.3·§44.4는 이 저장소의 §5·§20이 참조하는 저장소 전용 조항이라 전역 사본에는 없습니다.

> **2026-08-01 정리** — 모델이 기본으로 수행하는 절차(계획 분해·todo 강제·상태 외부화·작은 변경과 즉시 검증·자기 검증 루프·범위 준수·모델 배분 전략·goal 3역할 프로토콜)를 삭제했습니다. 그런 습관은 §1–§42 원 조항과 실행 툴의 기본 동작이 이미 강제하므로, 하네스가 그것을 다시 적기 시작하면 매 세션 토큰만 쓰고 보고에 상투구를 늘립니다. 지금 남은 것은 모델이 스스로 알 수 없는 취향·환경 사실·경험칙뿐입니다. 항목을 다시 늘리기 전에, 그것이 정말 기본 동작에서 벗어나는 것인지 먼저 확인하세요.

---

## 43. 모델 하네스 (Model-Tier Harness)

이 저장소의 작업 품질 기준은 **실행 모델의 등급과 무관하게 동일**합니다. 실행 모델이 무엇이든 §1–§42는 생략 없이 적용되며, 실행 모델의 등급을 낮은 품질·생략된 검증·축소된 범위의 사유로 삼지 않습니다. 규칙 본문의 단일 진실 공급원은 언제나 원 조항이고, 문구가 어긋나면 원 조항이 우선합니다.

읽고 나서 고치기, 단계별 검증, 범위 유지, 정직한 보고 같은 기본 습관은 원 조항(§3·§7·§14·§16·§40)이 이미 규정하므로 여기서 반복하지 않습니다. 아래는 모델이나 툴을 바꿔도 놓치기 쉬워 별도로 명시해 두는 항목입니다.

- **사실이 동의보다 먼저**: 확인한 사실이 사용자의 주장·진단과 다르면 맞장구치지 말고, 근거와 함께 반대 의견이나 대안을 제시합니다(§3).
- **검증을 통과시키려고 검증을 고치지 않습니다**: 테스트를 약화·삭제·skip 하거나 기댓값을 실제 출력에 맞춰 바꾸지 않습니다. 테스트 자체의 결함이 원인일 때만 그 근거를 커밋 메시지 또는 `History Document`에 기록한 뒤 고칩니다(§14).
- **"아마 될 것"으로 끝내지 않습니다**: 실패하면 로그 전체를 읽고 원인을 좁혀 재시도하되, 원인 분석 없이 같은 명령을 그대로 재실행하지 않습니다(§39). 같은 접근이 3회 실패하면 접근을 바꾸고, 그래도 안 되면 시도 내역·실패 로그·남은 가설과 함께 `BLOCKED`로 보고합니다(§26·§40).

---

## 44. 목표 완결과 완성 기준 (Goal Completion & Doneness)

이 섹션은 자동 완주(§2)의 반대편 함정 — 모든 아이디어를 활성 작업으로 만들고, 완료된 작업을 무한히 다시 여는 것 — 을 막는 규칙입니다. §5·§7의 범위 규칙과 §40의 완료 기준 위에서 적용합니다.

### 44.1 목표 완결 우선 (Primary Objective)

- 프로젝트 확장보다 현재 목표의 완결이 먼저입니다. 크고 사변적인 설계보다 작고 완결되고 검증 가능한 결과를 우선합니다.
- 유용해 보이는 아이디어를 전부 활성 기능·프로젝트로 만들지 않습니다. `Backlog Document`에 기록하는 것으로 충분합니다.
- `Goals Document`가 있으면 활성 목표의 작업만 수행합니다. 관련 코드를 수정 중이라는 이유로 미래 목표를 앞당겨 구현하지 않으며, 우선순위 변경은 사용자가 결정합니다.

### 44.2 Production-Ready 기본값

- 일회성 프로토타입을 명시적으로 요청받지 않는 한, 프로덕션 경로에 placeholder 구현·가짜 성공 응답·삼켜진 예외·문서화되지 않은 임시 동작을 남기지 않습니다(§6). 오류 처리, 보안·개인정보 통제, 데이터 무결성과 복구, 업그레이드·마이그레이션 경로, 운영상 제약의 문서화를 함께 봅니다.
- Production-ready는 **모든 미래 기능의 선구현이 아닙니다.** 프로덕션으로 가는 경로를 보존하는 점진적 목표를 사용합니다.

### 44.3 신규 발견 작업 분류 (Work Classification)

- 작업 중 발견한 새로운 작업은 **Blocker**(없이는 완료가 불가능·불안전) / **Current scope**(수용 기준이 직접 요구) / **Backlog**(유용하나 지금은 불필요) / **Rejected**(불필요·중복·유해) 중 하나로 분류합니다.
- Blocker와 Current scope만 진행 중인 작업을 확장할 수 있으며, Blocker는 확장 전에 사용자에게 설명합니다. Backlog는 `Backlog Document`에 기록만 하고, 명시적인 우선순위 지정 전에는 구현하지 않습니다(§7).

### 44.4 의사결정 정책 (Decision Policy)

- 되돌리기 쉬운 결정은 가장 단순하고 합리적인 선택지와 기존 관례로 빠르게 구현·검증하고, 장시간 비교하지 않습니다.
- 되돌리기 어려운 결정(보안, 데이터 형식, 공개 API, 인프라, 라이선스, 주요 의존성)은 현실적인 대안을 비교하고, 트레이드오프를 명시하고, `Decision Log`에 기록하고, 가능하면 마이그레이션·롤백 전략을 정의합니다.
- 새 증거가 전제를 실질적으로 바꾸지 않는 한 수용된 결정을 다시 열지 않으며, 더 새롭거나 흥미롭다는 이유만으로 동작하는 기술을 교체하지 않습니다.

### 44.5 아키텍처 최소주의 (Minimal Architecture)

- 확인된 요구사항을 안전하게 지탱하는 가장 작은 구조를 사용합니다. 한 번만 쓰이는 코드에 추상화·설정화·확장 포인트를 만들지 않습니다(§6).
- 확장 포인트는 현재 요구사항이 복수의 구현을 필요로 하거나, 알려진 단기 목표가 그 변형을 요구하거나, 나중에 바꾸면 상당한 마이그레이션 위험이 생길 때만 만듭니다. enterprise 답게 보이게 하려는 목적만으로 인터페이스·계층·서비스·모듈을 만들지 않습니다.

### 44.6 리뷰 상한 (Review Limits)

- 의도적인 리뷰는 최대 2회가 기본입니다 — 1차(§16 연계): 정확성, 수용 기준, 보안·데이터 안전, 실패 처리 / 2차(§33 연계): 유지보수성, 회귀 위험, 문서, 최종 검증. 예외는 치명적 결함 발견, 수용 기준 변경, 이전 결정을 무효화하는 새 증거뿐입니다.
- 2차 이후 발견되는 non-blocking 개선점은 완료된 작업을 다시 열지 말고 `Backlog Document`로 보냅니다. 안심을 얻으려는 목적만으로 광범위한 리뷰를 반복하지 않습니다.

### 44.7 완료의 유효성 (Completion Validity)

- §40 충족, 치명적 결함 없음, 남은 non-blocking 작업의 별도 기록, 빌드·복구 가능한 프로젝트 상태면 완료입니다. **완벽함은 완료 기준이 아니며, 발견된 개선점이 완료를 자동으로 무효화하지 않습니다.**
- 수용 기준을 충족했고 남은 발견 사항이 전부 non-blocking이면, 추가 아이디어는 `Backlog Document`로 보내고 현재 작업을 마무리합니다(§44.3·§44.6). 위험 상황에서 멈추는 규칙은 §26이고, 이 조항은 잘 되어가는 작업의 확장을 멈추는 규칙입니다.

### 44.8 제품 가치와 운영 현실 (Product & Operational Reality)

- 기술적 가능성이 제품 가치를 증명하지 않습니다. 실질적인 새 기능을 만들기 전에 사용자 문제, 문제가 실재한다는 근거, 성공 측정 방법, 가장 저렴한 검증 방법을 식별하고, 불확실한 가정의 사용자 검증을 회피하기 위해 엔지니어링을 더 쌓지 않습니다.
- 인프라 비용이 현재 0이라는 이유로 서비스를 "사실상 무료"라고 설명하지 않습니다. 지원 부담, 남용 방지, 모니터링, 백업, 장애 대응, 플랫폼 정책 변화, 데이터 보존, 법적 책임, 유지보수 시간을 함께 고려하고, 신뢰성·규모·모더레이션 요구가 무비용 제약과 충돌하면 트레이드오프를 직접 설명합니다.
